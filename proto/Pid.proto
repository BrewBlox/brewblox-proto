syntax = "proto3";

import "brewblox.proto";
import "nanopb.proto";
import "SetpointSensorPair.proto";

package blox.Pid;

message Block {
  option (brewblox.msg).objtype = Pid;
  option (brewblox.msg).impl = EnablerInterface;

  optional uint32 inputId = 1 [
    (brewblox.field).objtype = SetpointSensorPairInterface,
    (brewblox.field).stored = true
  ];
  optional uint32 outputId = 2 [
    (brewblox.field).objtype = ActuatorAnalogInterface,
    (brewblox.field).stored = true
  ];

  optional sint32 inputValue = 5 [
    (brewblox.field).unit = Celsius,
    (brewblox.field).scale = 4096,
    (brewblox.field).logged = true,
    (brewblox.field).logged_decimals = 3,
    (brewblox.field).readonly = true,
    (nanopb).int_size = IS_32
  ];
  optional sint32 inputSetting = 6 [
    (brewblox.field).unit = Celsius,
    (brewblox.field).scale = 4096,
    (brewblox.field).logged = true,
    (brewblox.field).logged_decimals = 3,
    (brewblox.field).readonly = true,
    (nanopb).int_size = IS_32
  ];
  optional sint32 outputValue = 7 [
    (brewblox.field).scale = 4096,
    (brewblox.field).logged = true,
    (brewblox.field).logged_decimals = 2,
    (brewblox.field).readonly = true,
    (nanopb).int_size = IS_32
  ];
  optional sint32 outputSetting = 8 [
    (brewblox.field).scale = 4096,
    (brewblox.field).logged = true,
    (brewblox.field).logged_decimals = 2,
    (brewblox.field).readonly = true,
    (nanopb).int_size = IS_32
  ];

  optional bool enabled = 11
      [ (brewblox.field).stored = true, (brewblox.field).logged = true ];
  bool active = 12
      [ (brewblox.field).logged = true, (brewblox.field).readonly = true ];

  optional sint32 kp = 13 [
    (brewblox.field).unit = InverseCelsius,
    (brewblox.field).scale = 4096,
    (brewblox.field).stored = true,
    (nanopb).int_size = IS_32
  ];
  optional uint32 ti = 14 [
    (brewblox.field).unit = Second,
    (brewblox.field).stored = true,
    (nanopb).int_size = IS_16
  ];
  optional uint32 td = 15 [
    (brewblox.field).unit = Second,
    (brewblox.field).stored = true,
    (nanopb).int_size = IS_16
  ];

  optional sint32 p = 16 [
    (brewblox.field).scale = 4096,
    (brewblox.field).logged = true,
    (brewblox.field).logged_decimals = 2,
    (brewblox.field).readonly = true,
    (nanopb).int_size = IS_32
  ];
  optional sint32 i = 17 [
    (brewblox.field).scale = 4096,
    (brewblox.field).logged = true,
    (brewblox.field).logged_decimals = 2,
    (brewblox.field).readonly = true,
    (nanopb).int_size = IS_32
  ];
  optional sint32 d = 18 [
    (brewblox.field).scale = 4096,
    (brewblox.field).logged = true,
    (brewblox.field).logged_decimals = 2,
    (brewblox.field).readonly = true,
    (nanopb).int_size = IS_32
  ];

  optional sint32 error = 19 [
    (brewblox.field).unit = DeltaCelsius,
    (brewblox.field).scale = 4096,
    (brewblox.field).logged = true,
    (brewblox.field).logged_decimals = 3,
    (brewblox.field).readonly = true,
    (nanopb).int_size = IS_32
  ];
  sint32 integral = 20 [
    (brewblox.field).scale = 256,
    (brewblox.field).logged = true,
    (brewblox.field).logged_decimals = 0,
    (brewblox.field).readonly = true,
    (nanopb).int_size = IS_32
  ];

  sint32 derivative = 21 [
    (brewblox.field).scale = 524288,
    (brewblox.field).logged = true,
    (brewblox.field).readonly = true,
    (nanopb).int_size = IS_32
  ];

  // when non-zero, resets integral to make i equal to what has been sent
  optional sint32 integralReset = 23 [
    (brewblox.field).scale = 4096,
    (nanopb).int_size = IS_32
  ];

  optional sint32 boilPointAdjust = 24 [
    (brewblox.field).unit = DeltaCelsius,
    (brewblox.field).scale = 4096,
    (brewblox.field).stored = true,
    (nanopb).int_size = IS_32
  ];
  optional sint32 boilMinOutput = 25 [
    (brewblox.field).scale = 4096,
    (brewblox.field).stored = true,
    (nanopb).int_size = IS_32
  ];
  bool boilModeActive = 26
      [ (brewblox.field).logged = true, (brewblox.field).readonly = true ];

  SetpointSensorPair.FilterChoice derivativeFilter = 27
      [ (brewblox.field).readonly = true ];

  optional SetpointSensorPair.FilterChoice derivativeFilterChoice = 28
      [ (brewblox.field).stored = true ];

  optional sint32 kff = 29 [
    (brewblox.field).unit = InverseCelsius,
    (brewblox.field).scale = 4096,
    (brewblox.field).stored = true,
    (nanopb).int_size = IS_32
  ];

  optional sint32 ff = 30 [
    (brewblox.field).scale = 4096,
    (brewblox.field).logged = true,
    (brewblox.field).logged_decimals = 2,
    (brewblox.field).readonly = true,
    (nanopb).int_size = IS_32
  ];

  optional uint32 ambientId = 31 [
    (brewblox.field).objtype = TempSensorInterface,
    (brewblox.field).stored = true
  ];

  optional sint32 ambientValue = 32 [
    (brewblox.field).unit = Celsius,
    (brewblox.field).scale = 4096,
    (brewblox.field).logged = true,
    (brewblox.field).logged_decimals = 3,
    (brewblox.field).readonly = true,
    (nanopb).int_size = IS_32
  ];

  optional sint32 ambientOffset = 33 [
    (brewblox.field).unit = DeltaCelsius,
    (brewblox.field).scale = 4096,
    (brewblox.field).logged = true,
    (brewblox.field).logged_decimals = 3,
    (brewblox.field).readonly = true,
    (nanopb).int_size = IS_32
  ];

  optional sint32 smoothGain = 35 [
    (brewblox.field).scale = 16384,
    (brewblox.field).logged = true,
    (brewblox.field).logged_decimals = 3,
    (brewblox.field).readonly = true,
    (nanopb).int_size = IS_16
  ];

  // Smoothing band for proportional/derivative blending
  optional sint32 smoothBand = 34 [
    (brewblox.field).unit = DeltaCelsius,
    (brewblox.field).scale = 4096,
    (brewblox.field).stored = true,
    (nanopb).int_size = IS_32
  ];

  // Deprecated field
  bool drivenOutputId = 90
      [ (brewblox.field).ignored = true, (nanopb).type = FT_IGNORE ];
}
